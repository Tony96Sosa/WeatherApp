import React from 'react';
import { render } from '@testing-library/react';
import ForecastItem from './ForecastItem';

test('ForecastItem render', async () => {
    // const { findByText } = render(<ForecastItem weekDay={'Lunes'} hour={11} state={'sunny'} temperature={16}/>)
    // const weekDay = await findByText(/Lunes/);
    // const hour = await findByText(/11/);
    // const state = await findByText(/sunny/);
    // const temperature = await findByText(/16/);
    // expect(weekDay).toHaveTextContent('Lunes');
    // expect(hour).toHaveTextContent('11');
    // expect(state).toHaveTextContent('sunny');
    // expect(temperature).toHaveTextContent('16');
})